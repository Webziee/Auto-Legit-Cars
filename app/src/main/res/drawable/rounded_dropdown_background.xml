<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="55dp"/>  <!-- Set the corner radius to make it rounded -->
    <solid android:color="@android:color/black"/>  <!-- Background color of the dropdown -->
    <stroke
        android:width="1dp"
        android:color="@android:color/black"/>  <!-- Optional: Add a border color -->
</shape>